enum LearningMode {
  standard, // Default – answers questions directly
  schoolHelp, // Guided learning – asks questions
}

enum SchoolSubject {
  math,
  reading,
  science,
  writing,
  general,
}

enum SchoolAgeGroup {
  younger, // 5-9 years
  older, // 10-14 years
}
